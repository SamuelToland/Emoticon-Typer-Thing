#######################
#	Global Variables
#######################
# Typing
ListenCommand = False 									# To know to listen for the following keys after prefix pressed
TypedCommand = "as"										# The possible command being typed to check against 'commands' array 
IgnoreKeys = 0 											# Used to ignore some auto key presses cuz screw python
FailSafeKey = "page_up"

# Key Presses
WordSelected = False 									# Represents if the command is being selected/highlighted in the current textbox
AllSelected = False 									# Represents if the whole textbox has been selected/highlighted
